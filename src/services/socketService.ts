// Realtime events are now handled directly via Supabase channels.
// This file is kept as a no-op stub so existing imports don't break.
export const socketService = {
  connect: () => {},
  disconnect: () => {},
  on: () => {},
  off: () => {},
  emit: () => {},
  joinRoom: () => {},
  leaveRoom: () => {},
};
